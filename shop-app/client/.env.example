# Staff sign-in for /login (override in production)
VITE_STAFF_PASSWORD=changeme

# Where Vite proxies /api in dev and preview (match shop-app/server PORT)
VITE_API_PROXY_TARGET=http://localhost:3001

# Optional (recommended for GitHub Pages): call API directly instead of same-origin /api
# Example:
# VITE_API_BASE_URL=https://your-backend-service.onrender.com
